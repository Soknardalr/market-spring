server:
  port: 8189
  servlet:
    context-path: /app

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/marketDB
    username: adm
    password: adm
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate